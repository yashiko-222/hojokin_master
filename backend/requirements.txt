# バックエンド（FastAPI）用の依存関係
fastapi==0.111.0
uvicorn==0.30.1
requests==2.32.3
beautifulsoup4==4.12.3
janome==0.5.0
scikit-learn==1.5.1
pydantic==2.8.2

# HTMLパースは標準ライブラリ(html.parser)を使用するため lxml は不要。
# メモリ・ビルドサイズ削減のため依存から除外している。

# --- RAG（任意・高精度化オプション）---
# 未インストールでも TF-IDF に自動フォールバックして動作します。
# 意味ベクトル検索を有効化する場合のみインストールしてください。
# sentence-transformers==3.0.1
